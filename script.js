// Initialize Scroll Animations
AOS.init({
    duration: 1000,
    offset: 100,
});

// Typing Effect
var typed = new Typed('#typing', {
    strings: ['B.Tech Student', 'Web Developer', 'Tech Enthusiast'],
    typeSpeed: 50,
    backSpeed: 30,
    loop: true
});
